import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: 'white',
    borderRadius: 100,
    width: 'auto',
    padding: 10,
    justifyContent: 'center',
    alignSelf: 'center',
    marginTop: 20,
  },
  text: {
    color: 'white',
    fontWeight: '600',
    textAlign: 'center',
  },
});
